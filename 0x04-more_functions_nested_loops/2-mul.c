#include "main.h"
/**
 * mul - Multiply two integers
 * @x: The first number is multiply
 * @y: The second number is multiply with thee first number
 * Return: Return the result of multiply x by y
 */
int mul(int x, int y)
{
	return (x * y);
}
